"""TradeMind India: a broker-import trading journal, rule-compliance and process-score
subsystem living inside Signal Evaluator as its own namespace (separate database tables,
separate API prefix, separate UI tab) -- see app/trademind/schema.py for why it isn't just
bolted onto the existing signals/orders tables.
"""
